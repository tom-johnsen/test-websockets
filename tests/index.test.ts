describe("Dummy tests", () => {
    it("should assert that true is true", () => expect(true).toBe(true))
    it("should assert that false is false", () => expect(false).toBe(false))
    it("should assert that 1 is 1", () => expect(1).toBe(1))
    it("should assert that 0 is 0", () => expect(0).toBe(0))
    it("should assert that -1 is -1", () => expect(-1).toBe(-1))
})
